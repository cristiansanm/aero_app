<form method="post" action="{{ path('personal_delete', {'id': personal.id}) }}" onsubmit="return confirm('¿Deseas eliminar al {{personal.funcion.nombre}} {{personal.apellido}}?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personal.id) }}">
    <button class="btn btn-danger">Borrar</button>
</form>
